import { FiltDirectionPipe } from './filt-direction.pipe';

describe('FiltDirectionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltDirectionPipe();
    expect(pipe).toBeTruthy();
  });
});
